#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int *comp_dd(char *pat){
	int i,j;
	char *p,*text;
	int len;
	int offset = 0;

	p = pat;
	text =pat;
	len = strlen(p);

	int *dd = (int *)malloc(sizeof(int)*len);
	int *next = (int *)malloc(sizeof(int*len));
	
	for(i = 0; i <= len -1; i++){
		dd[i] = len;
	}
	
	i = len -1;
	j = len;
	while(i >= 0){
		
	}
	return dd;
}
